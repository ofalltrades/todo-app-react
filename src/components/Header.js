import React, { Component } from 'react'
import styled from 'styled-components'

const Container = styled.div`
  text-align: center;
  font-size: 32px;
  padding: 1rem 0;
`

const Header = () => <Container>TODO NOTEPAD</Container>

export default Header
